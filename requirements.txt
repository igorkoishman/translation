# API
fastapi~=0.116.1
uvicorn~=0.35.0
aiofiles
python-multipart
python-dotenv~=1.1.1
requests
srt~=3.5.3
ffmpeg-python

# --- ASR stack (OpenAI Whisper + optional WhisperX alignment) ---
openai-whisper
# Torch CPU wheels (let pip pick a compatible version):
torch --index-url https://download.pytorch.org/whl/cpu
torchaudio --index-url https://download.pytorch.org/whl/cpu

# Alignment / extras (if you use your align step)
whisperx==3.4.2
transformers~=4.53.2
huggingface-hub~=0.33.4

# OCR / CV (since your Dockerfile installs tesseract)
pytesseract~=0.3.13
Pillow~=11.3.0
opencv-python-headless